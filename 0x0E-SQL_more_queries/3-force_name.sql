-- Creates the table force_name on your MySQL serverreates the table force_name on your MySQL server
-- Create table if not there
CREATE TABLE IF NOT EXISTS
force_name(
  id INT,
  name VARCHAR(256) NOT NULL
);
